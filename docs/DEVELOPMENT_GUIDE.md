# Development Guide

## Code Standards
- **Python**: Follow PEP 8 guidelines. Enforced via `ruff` and `black`.
- **TypeScript/React**: Next.js App Router patterns, functional components, strict typing. Enforced via `eslint` and `prettier`.
